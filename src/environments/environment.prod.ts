
export const environment = {
  production: true,
  apiURL: 'http://15.207.143.104/api',
  localServe: false, // set to false when not in local dev environment
  // baseUrl : window.location.origin + '/public/',
};
export const environment1 = {
  production: true,
  apiURL: 'http://15.207.143.104:9000/api',
  localServe: true, // set to false when not in local dev environment
};